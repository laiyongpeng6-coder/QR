{
  "enabled": true,
  "name": "智能审批门控",
  "description": "在执行写操作前，判断当前修改是否属于关键核心修改。如果是普通优化或小修改，直接执行不需要询问；只有涉及架构变更、核心逻辑修改、安全相关改动等关键修改时才暂停询问用户意见。",
  "version": "1",
  "when": {
    "type": "preToolUse",
    "toolTypes": [
      "write"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "判断当前即将执行的写操作是否属于\"关键核心修改\"。关键核心修改包括：架构层面的变更、核心业务逻辑的重大改动、安全相关的修改、数据库结构变更、API接口变更等。如果是普通的bug修复、代码优化、样式调整、添加注释、小功能实现等非关键修改，直接说\"ACCESS GRANTED\"允许执行。如果是关键核心修改，请暂停并向用户说明修改内容和原因，征求用户意见后再决定是否执行。"
  }
}